<ul>
  {{#each model as |post|}}
    <li>
      {{post.title}}
      <ul>
        {{#each post.tags as |tag|}}
          <li>
            {{tag.name}}
          </li>
        {{/each}}
      </ul>
    </li>
  {{/each}}
</ul>